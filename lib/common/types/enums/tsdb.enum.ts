export enum TSDB_PROVIDER {
    INFLUX = "influx",
    INFLUX2 = "influx2",
    PROMETHEUS = "prometheus"
}

export enum CONNECTION_STATUS {
    CONNECTED = "connected",
    FAILED = "failed"
}
